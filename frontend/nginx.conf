# Patient app: serves the built React SPA and proxies /api to the backend
# container, so the browser only ever talks to this one origin.
server {
    listen 8080;
    server_name _;
    server_tokens off;

    root /usr/share/nginx/html;
    index index.html;

    # The API caps uploads at 2 MB; reject anything much larger at the edge.
    client_max_body_size 3m;

    # Docker's embedded DNS, re-resolved so a restarted backend is still found.
    resolver 127.0.0.11 valid=10s ipv6=off;

    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;
    # Google's sign-in popup must be able to message this window.
    add_header Cross-Origin-Opener-Policy "same-origin-allow-popups" always;
    # Scripts only from this origin, Google Identity Services and Razorpay
    # checkout (plus its CDN): an injected <script> cannot run and read the JWT in
    # localStorage. Inline styles are needed by react-toastify and Google's button.
    add_header Content-Security-Policy "default-src 'self'; script-src 'self' https://accounts.google.com/gsi/client https://checkout.razorpay.com https://cdn.razorpay.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com https://accounts.google.com/gsi/style; font-src 'self' data: https://fonts.gstatic.com; img-src 'self' data: blob: https://res.cloudinary.com https://*.googleusercontent.com; connect-src 'self' https://accounts.google.com/gsi/ https://lumberjack.razorpay.com https://api.razorpay.com; frame-src https://accounts.google.com/gsi/ https://api.razorpay.com; object-src 'none'; base-uri 'self'; form-action 'self'; frame-ancestors 'none'" always;

    location /api/ {
        set $backend http://backend:4000;
        proxy_pass $backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        # Overwrite rather than append, so a client cannot smuggle in its own
        # X-Forwarded-For chain; the API trusts exactly one hop (TRUST_PROXY=1).
        proxy_set_header X-Forwarded-For $remote_addr;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Fingerprinted build assets can be cached for a long time.
    location /assets/ {
        expires 1y;
        try_files $uri =404;
    }

    # Client-side routes (e.g. /my-appointment) fall back to the SPA.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
